# Observability

Every execution is observable.

Metrics:

- tool latency
- plugin latency
- approval latency
- transport latency
- success rate
- failure rate

Tracing:

Every tool invocation creates a trace.

Logging:

Structured JSON logs.

Audit:

Immutable append-only records.

Observability data must never expose secrets.
