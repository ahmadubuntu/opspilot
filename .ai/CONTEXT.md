# Current Context

Repository bootstrap is complete.

Open WebUI is connected to LM Studio.

The runtime has not been implemented.

No plugins exist yet.

The first plugin will be Filesystem.

The project follows a plugin-based architecture.

The runtime must remain independent from plugins.

Future support includes remote agents communicating through MCP.
