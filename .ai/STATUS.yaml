project:
name: OpsPilot
version: 0.1.0
phase: "Phase 1.5 - Architecture Governance"

repository:
default_branch: main
current_work_branch: docs/spec-baseline-v1

architecture:
status: accepted

contracts:
version: v1

specifications:

```
tool:
  status: accepted

plugin:
  status: accepted

policy:
  status: accepted

approval:
  status: accepted

audit:
  status: accepted

workspace:
  status: accepted

transport:
  status: accepted

session:
  status: accepted

runtime_execution:
  status: accepted
```

governance:

spec_index: true

traceability: true

adr_index: true

architecture_reviews: true

current_vertical_slice:

implemented: false

scope:

```
- filesystem.read_file
- filesystem.list_directory
- filesystem.stat
```

phase2:

status: not_started

first_targets:

```
- core/errors.py
- core/models.py
- runtime/context.py
- runtime/dispatcher.py
- runtime/kernel.py
```

validation:

pytest: passing

ruff: passing

mypy: passing

next_branch:

feature/runtime-kernel-v1

next_milestone:

Runtime Execution Kernel

notes:

* Contracts v1 are accepted.
* Governance baseline is being finalized.
* Merge Gate has not yet been passed.
* Do not merge docs/spec-baseline-v1 until governance review is complete.

